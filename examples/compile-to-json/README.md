# Compile to JSON (end-to-end)

This example compiles the `SKILL.md` + `effector.toml` in this folder into raw Effector IR using the `json` compile target.

## Run

```bash
node compile.js
```

## Output

- `output/effector-ir.json`

